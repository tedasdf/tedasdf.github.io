


- MLA has more expresivie power than GQA under the same KV cache overhead

- have a seamlessly convert a GQA based pretrianed model to MLA keyt obstacle as RoPE , blocking the abvsorbv operaiont that Deepseek uses to swtich between compute and moemry efficent modes


- RoRoPE

- what is principle component analysis

- converts an existing GQA model into an MLA mdoel with only light fine tunnign, restroing acruacy wheil inherting MLA';s memory and speeed advantages

- optrimizer KV cache of exiitng pertrianed models , TransfMLA directly plugf into convest \\MHA2MLA determines which RoPE dimensions to remove solely based on the norms of theq   uesrty and key vector , induce larger informatio n lopss when pruning the same propritnoi of tpositoni 

the distribution of importnatciedmeinois is unven reuiqres sparse indexign tat compleicate optimisiotn and aceeleariont 




present TransMLA: motivated by two observaiotn:
WHY ??? 
1. for a fixed KV cache budge m MLA is stricly more expressive than GQA( any GQA layer can be rewritten as an MLA layer by
introducing a single additional projection matrix. The reverse transformation is not always possible,
implying that MLA subsumes GQA. When Rotary Positional Embeddings (RoPE) are present, the
MLA equivalent must be expressed in the absorbed form.
)
2. Inference acceleration occurs only when MLA uses a smaller KV cache


method : 
1. merging all key heads as one 
MLA ties all JKV heads to a single latent ion dimesnoin .  
step one is to convert a GQA to MLA merging every GQA key -value group into one latent head befroe anby KV cache compression is aplied 

For each query head i, we introduce WUK
i ∈ R
d×gd with the group index
j =
l
i
h/g m
− 1, and initialize the matrix WUK
i
[:, jd : (j + 1)d] to be Id (identity matrix of shape
d × d), with all other elements set to 0

basically only slelecitng pme group of qk 

we similarly initialize WUV
i
so that only the corresponding value head is an identity mapping, while all other elements are set
to zero. also selecitng one group of v 

merge RoPE from differenheads into one large RoPE 